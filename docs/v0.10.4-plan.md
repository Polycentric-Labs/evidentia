# Evidentia v0.10.4 — implementation plan

> **Status**: PLANNED (drafted 2026-05-23 at Step 5 of the v0.10.3
> pre-release-review). Forward-looking; concrete sequencing depends
> on Allen's direction after the v0.10.3 ship + the operator deep-
> dive Allen reserved for the v0.10.x close.
>
> **Theme**: YAML-catalog polish + carry-overs from the v0.10.x line
> + operator-deep-dive-surfaced fixes (TBD).

---

## 1. Context

v0.10.3 shipped the YAML-catalog support (Phase 1) + the Gemara
reference-model mapping (Phase 2). The full v4 `/pre-release-review`
returned PROCEED-CLEAN with 0 findings from `/security-review` and
4 polish-class suggestions from `/code-review` — all deferred here.

After the v0.10.3 ship, Allen will conduct the **operator deep-dive**
(reserved at v0.10.2 cycle entry) — wiki/walk-through/demos/testing
of every Evidentia surface from an operator perspective. Findings
from that pass land in this v0.10.4 cycle alongside the polish
items.

## 2. Polish items carried from v0.10.3 /code-review

### P1 — Module-level docstring choke-point note (loader.py)

- **Source**: v0.10.3 `/code-review` suggestion #1.
- **Scope**: One-line note in `evidentia_core.catalogs.loader`'s
  module docstring stating that **all** catalog loaders MUST go
  through `_load_catalog_data` — don't add a sibling `json.loads`
  or `yaml.safe_load` call elsewhere. Hardens the choke point
  against future-contributor drift.
- **Effort**: Trivial (1-line docstring edit).
- **Risk**: None.

### P2 — Error-message polish for no-extension catalog paths (loader.py)

- **Source**: v0.10.3 `/code-review` suggestion #2.
- **Scope**: When `_load_catalog_data` hits the unsupported-extension
  branch with `suffix == ''` (operator drag-and-drop without an
  extension), mention the extension allowlist explicitly in the
  error message. Current message is clear but doesn't tell the
  operator the fix is "rename to .yaml or .json".
- **Effort**: Trivial (1-line error-message edit).
- **Risk**: None.

### P3 — `framework_id` collision check in regenerate_manifest.py

- **Source**: v0.10.3 `/code-review` suggestion #3.
- **Scope**: After `scan_dir` builds its `entries` list for a tier
  directory, assert no two entries share the same `framework_id`.
  Today's `TIER_DIRS` config makes a collision extremely unlikely
  (would require a deliberate same-name `foo.json` + `foo.yaml`
  pair), but defensive guard is cheap and would catch a
  contributor who forgets to delete the JSON when converting to
  YAML.
- **Effort**: Small (~10 LOC + 1 test).
- **Risk**: None — failure mode is a clear `ValueError` at
  manifest-regen time, before the manifest ships.

### P4 — Multi-line round-trip test coverage (test_yaml_loader.py)

- **Source**: v0.10.3 `/code-review` suggestion #4.
- **Scope**: Extend `test_yaml_and_json_load_to_identical_catalogs`
  to cover controls with multi-line `description`, populated
  `assessment_objectives` and `parameters`. The current 7-control
  iso-27017-2015 stub uses single-line strings, so byte-equivalent
  round-trip is asserted indirectly via the registry test. A
  targeted multi-line round-trip would harden the proof for the
  first contributor who hand-authors a multi-line YAML catalog
  (the use case YAML is intended to enable).
- **Effort**: Small (~30 LOC test + fixture).
- **Risk**: None — pure additive test coverage.

### P5 — Pre-tag CHANGELOG-presence check (release.yml or pre-release-review skill)

- **Source**: v0.10.3 SHIP INCIDENT. `release.yml` first-fire on tag
  `v0.10.3 @ 487e0ac` succeeded at PyPI publish (7 wheels with PEP
  740 + SLSA) but FAILED at the `Extract release body from CHANGELOG`
  step — I missed adding the `[0.10.3]` block to `CHANGELOG.md`
  during Step 6 of the v0.10.3 pre-release-review. Downstream skip
  blocked GitHub Release + GHCR container publishing. Recovery
  required a Plan A move-tag re-fire after backfilling the block.
- **Outcome (v0.10.4 implementation)**:
  - **Workflow layer SHIPPED**: new
    [`.github/workflows/verify-changelog.yml`](../.github/workflows/verify-changelog.yml)
    fires on every `push: branches: [main]` or `pull_request:
    branches: [main]` that touches `pyproject.toml` / `CHANGELOG.md`
    / `scripts/extract_changelog_block.py` / its own definition.
    Reads the workspace meta-package `[project] version` from
    `pyproject.toml` and runs `extract_changelog_block.py` against
    that version with the same 1500-byte size gate `release.yml`
    enforces. Failing here is cheap (re-author the CHANGELOG block
    in a follow-up commit); failing at tag time is expensive (PyPI
    publish is irreversible). Catches the v0.10.3 incident class
    in CI before any tag fires.
  - **Skill layer DEFERRED**: edits to
    `~/.claude/skills/pre-release-review/` (global skill prose for
    Step 6.C + the 16-row pre-push gate Row 17) were blocked by
    the auto-mode classifier as "Self-Modification of agent global
    skill configuration" not covered by the v0.10.4 "complete P1-P5"
    project-level approval. Status: needs Allen's explicit per-action
    approval to add the skill-layer prose; until then the workflow
    layer carries the entire P5 mitigation. The skill-layer addition
    is a nice-to-have (operator sees the gate earlier, at tag-prep
    time on their local machine), but the workflow-layer alone
    closes the incident-class because CI fails before the tag can
    fire.
- **Effort**: ~85 LOC workflow file + this plan update. Skill prose
  TBD.
- **Risk**: None — pure additive gate.
- **Related**: the underlying `extract_changelog_block.py` script
  already enforces the 1500-byte sanity gate at workflow runtime;
  the new workflow lifts that check to PR-time so the gate failure
  is loud earlier and recovery is cheaper (no need to move a tag).

## 3. Operator deep-dive backlog (placeholder)

To be populated after Allen's operator-deep-dive pass. Expected
shape: surface-by-surface findings on:

- CLI ergonomics (verbs, flag-naming, error messages)
- REST endpoint shapes (envelope vs bare array, status codes)
- MCP tool surface coverage gaps
- Documentation freshness + walk-through quality
- Catalog coverage gaps (which frameworks are missing?)
- Evidence-collection coverage gaps (which data sources are
  missing?)

Allen will likely surface 5–20 findings ranging from documentation
typos to small-feature gaps. Most should be in-scope for v0.10.4;
larger surfaces (new collector, new framework, new REST resource)
get re-prioritized into v0.10.5 or v0.11.

## 4. Out of scope (deferred further)

- Upstream the marketplace plugin PR (v0.10.3 Candidate A) —
  separate publishing action; needs explicit approval per the
  publishing-authority protocol.
- Persona modes (v0.10.3 Candidate B) — pending operator-deep-dive
  evidence that they earn the surface-area cost.
- v1.0 acceptance gates (multi-reviewer walk-throughs, OpenSSF
  Gold, cryptographic CIMD signatures wired at FastMCP dispatch)
  per `docs/v1.0-transition.md`.
- Pro / Federal commercial-tier launch — post-v1.0.

## 5. Release mechanics

Bump shape: **patch** (v0.10.3 → v0.10.4) unless the operator-
deep-dive surfaces something feature-shaped enough to warrant a
minor (in which case v0.11.0 is defensible). Allen's standing
preference for full `/pre-release-review` applies regardless.
