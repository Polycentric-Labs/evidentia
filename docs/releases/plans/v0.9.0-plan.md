# v0.9.0 plan — Federal compliance

> Status: **shipped TBD**. First minor of the v0.9.x line. Sister
> docs: [`poam-runbook.md`](../../poam-runbook.md),
> [`conmon-runbook.md`](../../conmon-runbook.md),
> [`threat-model.md`](../../threat-model.md),
> [`capability-matrix.md`](../../capability-matrix.md).

## Theme

*"Federal compliance — POA&M lifecycle + CONMON cycle calendar +
walk-through-as-validation."* First minor since the v0.8.x line
closed (v0.8.0 → v0.8.7; 8 releases over 3 days). Lands the
federal-compliance theme reserved at v0.8.7 cycle-close, opening
the v0.9.x line with operator-facing surfaces auditors expect to
see in any regulated-industry GRC tool: Plan-of-Action-and-
Milestones tracking + Continuous Monitoring cycle calendar.

## What shipped

### Phase 1 — POA&M data layer + state model

- **NEW `evidentia_core.models.gap.POAMState`** — 5-state enum
  (`planned` / `in_progress` / `overdue` / `completed` /
  `verified`) aligned to the FedRAMP POA&M Template Completion
  Guide v3.0 + NIST SP 800-53A Rev 5 Appendix F vocabulary.
  Forward-only transition rules; backward transitions
  programmatically blocked to preserve auditor-defensible
  monotonic progress.
- **NEW `evidentia_core.models.gap.Milestone`** — Pydantic record
  carrying `target_date` + `description` + `status` + optional
  `evidence_ref` + `created_at` + `updated_at`. UUID v4 ID stamp
  for OSCAL POA&M back-matter cross-reference. Description
  bounded at 2048 chars; evidence_ref at 512.
- **NEW `ControlGap.poam_milestones: list[Milestone]`** optional
  field with default-empty for backward-compat with v0.7.x +
  v0.8.x serialized gap reports — Pydantic adds the empty list on
  parse.
- **NEW `evidentia_core.poam`** sub-package:
  - `state.py` — `is_valid_transition` + `valid_next_states` +
    `TERMINAL_STATES` + `derive_overdue` predicate (`target_date
    < today` AND status ∈ {PLANNED, IN_PROGRESS})
  - `milestone.py` — pure-function cycle helpers
    (`sort_milestones_by_target_date` +
    `group_milestones_by_state` + `upcoming_milestones` +
    `derive_attention_state`)
- **NEW `evidentia_core.poam_store`** — JSON file-store mirroring
  v0.7.9 P0.1.2 `vendor_store`: atomic-write + UUID-shape ID
  gate + belt-and-suspenders `validate_within` path-traversal
  check + `EVIDENTIA_POAM_STORE_DIR` env override + platformdirs
  default. Refreshes `Milestone.updated_at` on persisted state
  changes vs the on-disk version.
- **NEW 6 EventActions** in `evidentia_core.audit.events`:
  `POAM_CREATED` / `POAM_UPDATED` / `POAM_MILESTONE_REACHED` /
  `POAM_OVERDUE` / `POAM_CLOSED` / `POAM_VERIFIED`.
- `docs/log-schema.md` — new `evidentia.poam.*` section
  documenting all 6 actions + common evidentia-extension fields
  per the AU-3 contract.

### Phase 2 — POA&M CLI + REST + OSCAL emit

- **NEW `evidentia poam` Typer subcommand group** (7 verbs):
  - `create --from-gap-report <path>` — auto-materializes POA&M
    items from a saved gap report. Default policy materializes
    only CRITICAL + HIGH severity gaps (FedRAMP POA&M Template
    Completion Guide v3.0 §3.1 auditor-default); `--all` opts
    into the full set; `--overwrite` replaces existing records.
  - `list [--all] [--severity csv] [--json]` — canonical sort
    (severity rank → has-open-milestones → earliest-open-target-
    date → control_id)
  - `show <poam-id> [--json]` — human-readable detail
  - `update <poam-id> --status/--assigned-to/--remediation-
    guidance/--add-tag/--remove-tag` — top-level field edits;
    `--status=remediated` additionally fires `POAM_CLOSED`
  - `milestone add <poam-id> --target-date <d> --description
    <d> [--status S] [--evidence-ref R]`
  - `milestone update <poam-id> <ms-id>` — state-machine
    enforced; backward transitions blocked with a clear "file a
    NEW milestone with a fresh target_date" message
  - `delete <poam-id> [--yes]` — interactive prompt by default
  - `calendar [--window-days N] [--today YYYY-MM-DD] [--json]` —
    read-only attention surface across all POA&Ms

- **NEW `/api/poam/*` FastAPI router** (8 endpoints) mirroring
  v0.7.9 TPRM router shape + inheriting v0.7.8 F-V08-DAST-3
  error-normalization (400 for runtime body-content; 404 for
  shape-violation + not-found IDs):
  - `GET    /api/poam/items`
  - `POST   /api/poam/items`
  - `GET    /api/poam/items/{poam_id}`
  - `PUT    /api/poam/items/{poam_id}`
  - `DELETE /api/poam/items/{poam_id}`
  - `POST   /api/poam/items/{poam_id}/milestones`
  - `PATCH  /api/poam/items/{poam_id}/milestones/{milestone_id}`
  - `GET    /api/poam/calendar`

- **NEW `evidentia_core.oscal.poam_exporter`** ships
  `gap_report_to_oscal_poam()` — emits OSCAL 1.1.2
  `plan-of-action-and-milestones` JSON. Each `ControlGap` →
  one (observation, risk, poam-item) triple with UUID cross-
  references resolved via `gap.id`. Milestones emit as
  `tracking-entries` under `risks[].remediations[]`. Evidentia-
  namespaced `status` + `target-date` + `evidence-ref` props
  live under `ns=https://evidentia.dev/oscal` so trestle-
  conformance round-trips preserve them as opaque extensions.
  Back-matter integrity: each POA&M record's canonical JSON is
  base64-encoded in `back-matter.resources[].base64.value` with
  SHA-256 in `rlinks[].hashes[]` — mirrors the v0.7.0 finding-
  resource embedding pattern so tampering with an embedded
  record fails the chain-of-custody check.

### Phase 3 — CONMON cycle calendar (read-only)

- **NEW `evidentia_core.conmon`** pure-function library:
  - `ConmonCadence` Pydantic model (slug + framework + activity
    + frequency + description + citation) — slugs stable across
    releases, append-only for additions, never renamed
  - `CadenceFrequency` enum (`monthly` / `quarterly` / `annual`
    / `biennial` / `triennial`) + month-delta map
  - `CycleAttentionState` enum (`current` / `due_soon` /
    `overdue`) — mirrors v0.9.0 P1 POA&M attention vocabulary
    so operator UIs render both signals through the same
    widgets
  - **7 bundled cadences**: NIST 800-53 CA-7 (monthly) +
    FedRAMP ConMon × 3 (monthly POA&M + monthly scans + annual
    SAR) + CMMC L2 triennial + DoD RMF annual + OCC 2026-13a
    model-risk annual. Each carries the regulatory citation.
  - `next_due()` — calendar-aware + last-day-clamping month
    arithmetic (e.g., `2026-01-31 + 1 month → 2026-02-28`;
    never produces invalid dates)
  - `derive_status()` — bucket a pending cycle into the 3-state
    attention model against a reference date
  - `register_cadence()` — process-local runtime extension for
    organization-specific cycles; durable extensions land via
    the v0.8.0 P0.4 plugin-contract surface (v0.9.1 if walk-
    through identifies operator demand)

- **NEW `evidentia conmon` CLI** (3 verbs):
  - `list [--framework FW] [--json]`
  - `next <slug> --last-completed YYYY-MM-DD [--json]`
  - `check --last-completed-file state.yaml [--today Y]
    [--window-days N] [--json]` — fires audit events per due-
    soon + overdue cycle surfaced

  No daemon. Operators poll. The CONMON live-trigger daemon
  (`evidentia conmon watch`) is reserved for v1.0.

- **NEW 2 EventActions** in `evidentia_core.audit.events`:
  `CONMON_CYCLE_DUE` + `CONMON_CYCLE_OVERDUE`. Audit emit
  happens at the query layer, not in the library, so a given
  cycle can be "due" multiple times in a row without
  multiplying audit records.

- **NEW operator runbooks**:
  - [`docs/poam-runbook.md`](../../poam-runbook.md) — end-to-end
    POA&M workflow (bootstrap → milestones → attention checks
    → close-out → OSCAL emit → audit-trail interpretation)
  - [`docs/conmon-runbook.md`](../../conmon-runbook.md) — CONMON
    workflow (discover → compute next-due → state-file polling
    → CI integration → operator-extensible cadences → audit
    interpretation)

  Both cross-link + cover the POA&M / CONMON composition
  pattern via the shared attention-state vocabulary.

### Phase 4 — Walk-through-as-validation

> Status varies per release. The plan accepts both outcomes:
>
> - **If walk-through ran**: 3-5 federal-SI scenario rows added
>   to [`capability-matrix.md`](../../capability-matrix.md) under the
>   v0.9.0 snapshot; Cohen's Kappa recomputed on the v0.8.5
>   DFAH calibration corpus with a domain-expert second rater
>   (closes the v0.8.6 §29 P2 R3 mitigation acceptance).
> - **If walk-through deferred**: v0.9.0 ships with the v0.8.6
>   "single-rater κ probe inconclusive" carry-forward
>   acknowledged in [`inter-rater-agreement.md`](../../../tests/data/dfah-calibration/inter-rater-agreement.md);
>   the walk-through becomes a v0.9.1 reservation per §31.A
>   POA&M-first / walk-through-as-validation triage.

Refinement landing alongside Phase 3: CLI JSON output now goes
through `typer.echo()` rather than `rich.console.Console.print()`
to avoid terminal-width wrapping that corrupted JSON in
`CliRunner` output. Applied uniformly to `cli/conmon.py` +
`cli/poam.py`.

## Quality gates at ship

- **pytest**: 2575+ passed (was 2386 at v0.8.7; +189 over the
  Phase 1 + 2 + 3 deliverables)
- **mypy strict**: 0 errors across 227+ source files (was 217
  at v0.8.7; +10 new modules: poam package + poam_store +
  conmon package + cli/poam + cli/conmon + routers/poam +
  oscal/poam_exporter)
- **ruff**: clean
- **Standing-rule sweep**: clean across all v0.9.0-cycle commits
- **Author attribution**: Allen Byrd only (no Claude
  attribution markers)

## Pre-release-review v4 cadence

**Pre-tag (full 7-step)** — appropriate for v0.9.0's substantial
new public-surface introduction. Triggers all 4 `/code-review`
auto-fires per v4 G2:

1. New public API/CLI/route — `/api/poam/*` + `evidentia poam`
   + `evidentia conmon`
2. New file under `packages/*/src/` — `poam/`, `poam_store.py`,
   `conmon/`, `cli/poam.py`, `cli/conmon.py`,
   `routers/poam.py`, `oscal/poam_exporter.py`
3. >500 LOC delta — ~3000 LOC across Phase 1 + 2 + 3
4. Security-relevant subsystem touched — OSCAL POA&M emit
   handles audit-grade artifact emission with back-matter
   integrity protection

Mandatory `/security-review` at Steps 3 + 4 + 6.C per v4 G12.
Threat-model freshness check (G5) verified ≤ 180 days. 5th
canonical Pre-tag deliverable
`docs/security-review-v0.9.0.md`.

## What's outstanding (operator-driven; not Claude-blocking)

- **Phase 4 walk-through**: scheduling is operator-driven. The
  capability-matrix snapshot lands the federal-SI scenario
  rows IF the walk-through runs before ship; otherwise the
  walk-through becomes a v0.9.1 follow-up (§31.A explicitly
  accepts both paths).
- **Cohen's Kappa recompute**: same dependency — needs the
  domain-expert second rater on the v0.8.5 corpus.

## v1.0 carries-forward (per `v1.0-transition.md`)

Unchanged from v0.8.x cycle close:

- CONMON live-trigger daemon (`evidentia conmon watch`)
- Cryptographic CIMD signatures (per Webscale OIDC profile)
- API-stability commitment + frozen public surfaces +
  deprecation cycle policy
- OpenSSF Best Practices Badge Gold tier (requires ≥ 2
  contributors)
