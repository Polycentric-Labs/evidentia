# v1.0 transition narrative — DRAFT

> **Status**: DRAFT for stakeholder review; not a release
> commitment. Authored during v0.8.6 P5 as input to a future
> v0.9.0 / v1.0 cycle-open conversation.
>
> **Currency (refreshed 2026-08-20, v0.12 freeze-prep)**: the
> narrative below is a **v0.8.5-era snapshot** and is preserved as
> written — it is the record of what the v1.0 framing looked like
> when it was authored, and rewriting it would destroy that. Read
> it as history. Two things have moved since and are updated in
> place:
>
> - **The acceptance gates in § Acceptance gates for v1.0 now
>   carry their real status** (three are met), instead of the
>   all-unchecked list this document carried from v0.8.6 through
>   v0.11.2.
> - **Candidate B's "API stability commitment" is no longer a
>   proposal.** [api-stability.md](api-stability.md) was promoted
>   to NORMATIVE at v0.9.7 and has been binding since; the surfaces
>   it does *not* yet cover are enumerated in
>   [v1.0-freeze-candidates.md](v1.0-freeze-candidates.md).
>
> Everything else — the v0.8.x snapshot counts, the theme
> candidates, the timing estimate — is as-authored and should be
> read against `CHANGELOG.md` for current numbers.

## Where v0.8.x leaves us (snapshot at v0.8.5 close)

By v0.8.5, Evidentia's library + CLI surface had stabilized:

- **6 minor releases of v0.8.x** shipped (v0.8.0 → v0.8.5)
- **12 consecutive PROCEED-CLEAN** pre-release-reviews
  spanning the v0.7.x → v0.8.x line
- **The OSS-native AI moat is shipping**: DFAH determinism +
  faithfulness + replay equivalence (v0.8.0/2/4); Policy
  Reasoning Traces (v0.8.0 + v0.8.1 LLM-driven); MCP server
  + 4 tools + CIMD richness (v0.8.0 + v0.8.5); plugin-contract
  scaffolding (v0.8.0 P0.4)
- **Supply-chain posture is structural**: PEP 740 + Sigstore +
  SLSA L3 + cosign + osv-scanner + Trivy + Scorecard +
  CycloneDX SBOM all run on every release; G4 Path 2
  hash-pinning is structurally permanent; Scorecard
  PinnedDependencies = 10/10
- **Test count**: 2338 passing (was ~965 at v0.7.0 = +142%
  growth in 11 days)
- **mypy strict**: 0 errors across 216 source files; ruff
  clean; standing-rule sweep clean
- **OpenSSF Best Practices Badge**: Silver tier MET (Passing
  + Silver shipped pre-v0.8.5)

## What v1.0 stands for (working theme candidates)

The 2026-04-28 §10 Q4 lock-in reserved v0.9.0 for
**federal-compliance theme**: CONMON cycle calendar + OSCAL
POA&M emit + walk-through opener with a domain expert.

v1.0 itself sits AFTER v0.9.0 in the natural sequence. Two
candidate theme framings, each pulled forward to the v1.0
narrative:

### Candidate A — "Federal compliance shipped"

v1.0 = "v0.9.0 federal-compliance theme delivered + tested
against real federal-SI workflows + accepted by an external
domain expert". Concretely:

- CONMON cycle calendar shipped (read-only library + CLI
  listing upcoming monthly / quarterly / annual control
  cadence per framework)
- OSCAL POA&M emit shipped (auto-generation from gap-analysis
  findings; manageable POA&M tracking primitives — state
  model + milestones + cycle-diff)
- Domain-expert walk-through completed; capability-matrix.md
  expanded with real-world scenario rows
- 1+ federal-SI partner has run the v0.9.0 surface against
  their actual customer scenarios + provided feedback

### Candidate B — "API stability commitment"

v1.0 = "Public API contract is stable; semantic-versioning
guarantees apply". Concretely:

- Pydantic models in `evidentia_core.models.*` are public
  schema (frozen field names + types; non-breaking field
  additions only between v1.x releases)
- `EventAction` enum is public schema (additions only)
- CLI flags are public schema (deprecation cycles for
  breaking changes)
- Plugin contracts (`AuthProvider`, `StorageBackend`,
  `MarketplaceProvider`, `BaseSaaSCollector` shipped v0.8.0
  P0.4) are stable
- Library entry points (`evidentia_core.gap_analyzer.GapAnalyzer`,
  `evidentia_ai.risk_statements.RiskStatementGenerator`,
  `evidentia_eval.DFAHarness`, etc.) follow the same
  posture. (v0.10.5 P9: DFAH harness moved from `evidentia_ai.eval.*`
  to dedicated `evidentia-eval` workspace package; old path retained
  as deprecation shim through v0.11.x — removal in v0.12.0.)

Both candidates are non-mutually-exclusive. Most likely v1.0
combines them: ship the federal-compliance theme (Candidate A)
+ commit to the public API surface (Candidate B) at the same
time.

## What v1.0 will NOT do

The Allen-confirmed standing direction (per `~/.claude/CLAUDE.md`
+ the §0 absolute-secrecy posture re-confirmed multiple times
across the v0.7.x → v0.8.x cycle):

- **No commercialization vocabulary** on the public surface.
  v1.0 ships as OSS only; specific commercial-direction plans
  are not publicly committed pre-v1.0.
- **No GUI v2.** Web UI alpha.2 (v0.7.6) is the OSS UI
  posture. Surface upgrades happen via the existing alpha.2
  path; no separate "v1.0 UI" release.
- **No multi-tenant SaaS.** Per the standing rule + the
  threat models documented across the v0.8.x line, multi-
  tenant deployments are operator-responsibility (CIMD
  metadata + transport auth they wire); Evidentia itself
  doesn't host.
- **No hosted federal-cloud variant.** v0.9.0 federal-compliance
  ships the library + CLI primitives; FedRAMP-authorized
  hosting is downstream / out of scope for v1.0.

## API-stability commitments (Candidate B detail)

If v1.0 commits to API stability, the contract is:

### Frozen surfaces

- **Pydantic model field names + types** in
  `evidentia_core.models.*`. Adding optional fields is
  non-breaking; renaming or removing fields is a major-bump
  trigger.
- **`EventAction` enum values**. Adding new EventActions is
  non-breaking; renaming or removing values is a major-bump
  trigger.
- **CLI flag names + semantics**. Adding new flags is
  non-breaking; deprecation cycles cover renaming or removal.
- **Plugin contracts** (`AuthProvider.__init__` signature +
  `StorageBackend[T]` ABC method signatures + etc.).
- **Public library entry points** (`evidentia_core.*` +
  `evidentia_ai.*` + `evidentia_collectors.*` +
  `evidentia_integrations.*` + `evidentia_api.*` +
  `evidentia_mcp.*`).

### Non-frozen surfaces

- **Internal helper functions** (underscore-prefixed +
  documented as private).
- **Test fixtures** (operators don't import these).
- **Bundled catalog content** (catalogs evolve as
  authoritative sources update; per-catalog version pinning
  via `evidentia catalog pin <framework> <version>` is the
  operator escape hatch).
- **Threshold defaults** (operators tune via CLI flags;
  default-changes are documented but non-breaking).

### Deprecation cycle

- **Deprecation warning**: at minimum 1 minor-release cycle
  before removal.
- **Deprecation log entry**: every deprecation announced in
  CHANGELOG + ROADMAP.
- **Removal**: in the v(N+2).0 minor or later, never within
  a v(N).x patch.

## Acceptance gates for v1.0

Working candidate gates (subject to revision as v0.8.x
evidence accumulates).

**Status refreshed 2026-08-20 (v0.12 freeze-prep).** These boxes
sat unchecked from v0.8.6 through v0.11.2 even though three had
been satisfied for several releases — the list was never revisited
after authoring. Each entry below now states its evidence, and the
unmet ones say what actually blocks them.

- [x] **v0.9.0 federal-compliance theme shipped + accepted** —
  v0.9.0 shipped 2026-05-15 (CONMON cycle calendar, OSCAL POA&M
  emit); the federal line continued through v0.11.0's Wave 1–2
  (FedRAMP 20x KSI emit, M-25-21 practices, M-25-22 acquisitions).
  *"Accepted" here means shipped + reviewed, not externally
  validated — see the walk-through gate below.*
- [ ] Domain-expert walk-through completed + expanded
  capability-matrix — **BLOCKED**: a partner has been identified
  but not engaged. No federal-SI walk-through has taken place.
  This is the gate most likely to dominate the v1.0 calendar,
  since external-reviewer latency is outside our control; the
  reviewer protocol is being prepared ahead of outreach.
- [ ] 1+ external operator has run v0.9.x in production —
  **BLOCKED**: no external production deployment is evidenced.
- [x] **All public API surfaces documented in
  [`api-stability.md`](api-stability.md)** — the doc was promoted
  from DRAFT to **NORMATIVE at v0.9.7** (2026-05-19) and has been
  binding since; the original "(TBD doc)" parenthetical is
  retired. *Caveat, and the reason this gate is not simply
  closed*: v0.12.0's `scripts/check_public_surface.py` found four
  §5 frozen imports that had never resolved on any shipped
  release, and 43 live env vars outside the contract. The
  imports are corrected and now gated; the remaining delta is
  enumerated in
  [`v1.0-freeze-candidates.md`](v1.0-freeze-candidates.md) and
  must be dispositioned before v1.0.
- [x] **Deprecation calendar published for any v0.9.x →
  v1.0 changes** — [`deprecation-calendar.md`](deprecation-calendar.md)
  has been NORMATIVE since v0.9.7 and carries three active
  deprecations, all targeting v1.0.0. v0.12.0 executed its first
  scheduled removal (the `evidentia_ai.eval` shims) and made the
  M-24-10 deprecation machine-readable (RFC 8594 headers +
  `DeprecationWarning`).
- [ ] **REVISED 2026-05-25 per triple-validated procurement
  pressure-test (GPT-5.5 Fortune 500 procurement = 5/10;
  Grok-4.20-Multi-Agent federal-SI hostile TPRM = 2.5/10;
  Grok 4.3 heterodox marketing strategist = reject metaphor entirely):**
  the v1.0 acceptance-gate language now reads as the single
  procurement-ready paragraph in [positioning-and-value.md §10.4](positioning-and-value.md#104-the-acceptance-gate-paragraph-procurement-ready-fortune-500--federal-si-dual-audience).
  Material wording changes from the 2026-05-24 draft:
  - **Six positive supply-chain attestations LISTED FIRST** (PEP 740 +
    Sigstore + SLSA L3 + cosign + CycloneDX 1.7 + OpenSSF Scorecard
    6.5+), each explicitly mapped to NIST SP 800-218 SSDF section +
    NIST 800-53 SR/SA control + CISA SbD Pledge goal. Procurement
    officers evaluate against frameworks; the mapping converts each
    attestation into a vendor-questionnaire-defensible answer.
  - **OpenSSF Silver tier MET (currently met)** + **OpenSSF OSPS
    Baseline Maturity 2 conformance** (~95% conforming after Phase-B-
    driven v0.10.5 bootstrap; documented honest-gap for OSPS-AC-04.01/02
    + OSPS-QA-07 + OSPS-GV-04 — the three solo-maintainer-structurally-
    unreachable controls; verified at bestpractices.dev/en/criteria/2:
    "MUST have at least two unassociated significant contributors" +
    "Bus factor of 2 or more" — both absolute MUSTs with no exception
    path).
  - **SOC 2 Type I examination in progress** for Polycentric Labs LLC
    (Virginia) with scope locked to development lifecycle + release
    pipeline + vendor management + internal IT controls; observation
    period start date filled in when locked with the auditor; **NO
    committed report delivery date** (procurement-acceptable because it
    signals progress without promising what cannot be guaranteed).
  - **OpenSSF Gold structural ceiling acknowledgment** now **tied to
    the SOC 2 Type I program's segregation-of-duties + additional-
    governance controls**, NOT to a vague organizational-hiring
    milestone. This single rewording was the most material change
    from the 2026-05-24 draft: GPT-5.5 procurement reviewer flagged
    the prior phrasing as a red flag ("asking procurement to fund the
    vendor into having basic operational maturity"); the new framing
    reads as "we're addressing it through the same audit that produces
    the SOC 2 Type I" — procurement-acceptable.
  - **NO METAPHORS** (the prior "Terraform/dbt of GRC" frame in
    positioning-and-value.md §10 was dropped in the same 2026-05-25
    revision; OpenTelemetry-of-compliance + OSCAL-native-compliance-
    kernel alternatives were considered, pressure-tested, and
    rejected — direct enumerated claims only until reference customers
    exist).
  - **Polycentric Labs LLC (Virginia)** named explicitly as the
    accountable US-incorporated legal entity (closes the federal-SI
    procurement gate that Grok-4.20-MA flagged as disqualifying for
    any vendor lacking a US legal entity, FCL, or DCSA-registered
    facility regardless of paragraph quality).
- [ ] Pre-release-review v4 (or v5+) Pre-tag full 7-step
  PROCEED-CLEAN — **per-release gate**; satisfied at the v1.0.0
  tag itself, not before. Every release since v0.7.12 has met it.
- [ ] Step 7 post-tag verification ALL PASS — **per-release
  gate**, same as above; v0.11.2's post-tag verification passed
  in full (PyPI 8/8 with PEP 740 attestations, GHCR + cosign,
  Release assets).

## Open questions for v0.9.0 cycle-open

These shape the v1.0 narrative; resolutions land at v0.9.0
cycle-open:

1. **Walk-through scheduling**: when does the domain-expert
   walk-through happen? Best is BEFORE v0.9.0 scope-lock.
2. **Federal-SI partner**: is there a candidate organization
   to run v0.9.x against actual customer scenarios?
3. **Multi-rater corpus**: does v0.9.0 + the domain-expert
   walk-through naturally surface a second-rater for the
   DFAH calibration corpus (carrying forward the v0.8.6 P2
   κ-probe-inconclusive finding)?
4. **OSCAL POA&M scope**: emit-only + manageable-tracking
   primitives, or include CONMON cycle-diff at v0.9.0 scope?
5. **Open Source pledge**: v1.0 is also a natural moment to
   commit to the CISA Secure-by-Design Pledge (already
   documented in the v0.7.16 + v0.8.x security-review
   compliance mappings). Formal pledge ceremony at v1.0?

## Carries-forward into v1.0

Items reserved through the v0.8.x line that v1.0 inherits:

- **Multi-rater corpus pass + Cohen's Kappa ≥ 0.80** (v0.8.6 P2
  shipped κ-probe-inconclusive; v0.9.0 / v1.0 cycle adds the
  second human rater)
- **OpenSSF Best Practices Badge Gold tier** (requires ≥ 2
  contributors; v1.0 timing may align with the contributor
  threshold)
- **Signed MCP tool output** (historically mislabelled
  "cryptographic CIMD signatures": CIMD is an OAuth client-metadata
  document and signs nothing. The real mechanism, `SignedToolOutput`
  at the FastMCP dispatch layer, shipped v0.9.7 (envelope) and
  v0.9.8 (dispatch wrap); v1.0 attests the capability under this
  name)
- **Federal-compliance theme acceptance** (v0.9.0 ships the
  library work; v1.0 ships the acceptance evidence)

## v0.9.x milestones feeding into v1.0 (updated 2026-05-15)

v0.9.0 SHIPPED 2026-05-15. Remaining v0.9.x releases:

- **v0.9.1** — Walk-through-driven refinement (PLANNED)
- **v0.9.2** — AI governance foundation: ISO 42001 + NIST AI
  RMF + EU AI Act catalog enrichment + AI risk classification
  module + AI system inventory primitives (PROPOSED)
- **v0.9.3** — CONMON daemon poll-mode + basic alerting (PROPOSED)
- **v0.9.4** — Collaboration primitives: multi-user evidence
  store, assignments, evidence versioning, basic RBAC model
  (PROPOSED)

Each v0.9.x release progressively builds the foundation for
v1.0 API stability.

## Post-v1.0 direction (decided 2026-05-15)

v1.0 ships as pure OSS with API stability. The OSS core
(Apache 2.0) remains unchanged and complete. Any future
post-v1.0 direction — if and when it materializes — would
be separate from the v1.0 OSS release; specifics are not
publicly committed pre-v1.0.

This is consistent with the buyer-based open-core pattern
documented across the OSS-infrastructure landscape (GitLab,
Grafana, Snyk, Prowler, and others): a free engine + content
surface for individual contributors, with the post-v1.0
direction TBD per landscape evolution.

Timeline TBD; not blocking v0.9.x or v1.0 OSS work.

## Best-guess timing (as estimated at authoring time, v0.8.6)

v0.9.0 SHIPPED 2026-05-15. v0.9.x cycle has 3-4 remaining
releases. v1.0 sits 1-2 cycles past the final v0.9.x release
(1 cycle if walk-through + API-stability work lands cleanly;
2 cycles if v0.9.x surfaces issues).

Working estimate: **v1.0 ≈ Q3 2026 to Q4 2026** depending on
the v0.9.1 walk-through results and v0.9.2-4 pacing. NOT a
commitment; a working assumption.

## See also

- [`v0.7.x-retrospective.md`](releases/plans/v0.7.x-retrospective.md) — what
  the v0.7.x cycle taught us (input to this doc)
- [`ROADMAP.md`](ROADMAP.md) — release-by-release theme
  summary; v0.9.0 RESERVED section
- [`v0.8.6-plan.md`](releases/plans/v0.8.6-plan.md) — current cycle plan
  (P5 of which produced this DRAFT)
- [`positioning-and-value.md`](positioning-and-value.md) —
  market context that informs v1.0 theme candidates

---

*This is a DRAFT. Stakeholder review at v0.9.0 cycle-open
will refine + finalize.*
