# Evidentia v0.10.7 Implementation Plan

> **Status:** PLANNED (locked 2026-05-27).
> **Type:** patch — hygiene + automation-debt close-out + pre-push gate L2 + full wiki content fill.
> **Estimated scope:** ~3 weeks (revised 2026-05-27 after D6 expansion per
> Allen's "do it all" wiki directive). Compatible with the standing
> direct-push pattern on `Polycentric-Labs/evidentia` (Allen's 2026-05-26
> consent; in continuous use since v0.9.5).
> **Theme:** Last hygiene cycle on v0.10.x line before pivoting to v0.11
> federal-compliance theme. Closes v0.10.6 code-quality reviewer backlog
> (Groups A + D below), adds Pre-push Gate Layer 2 (blocking) per Phase 2
> spec, fills the full ~40-page wiki tree with triple-validation
> discipline, defers OSPS crosswalk SME hand-verification + Pre-push
> Gate L1/L3 to v0.10.8 or v0.11.
>
> **Execution pattern:** `superpowers:subagent-driven-development` —
> per-phase implementer subagent → spec-compliance reviewer → code-quality
> reviewer → fix-loop if findings. Direct-on-`main` per Evidentia's
> standing direct-push pattern.

---

## 1. Theme + scope rationale

v0.10.6 shipped clean (19th consecutive PROCEED-CLEAN), but its
post-cycle reviews surfaced ~9 code-quality items and the Step 7.D
verification surfaced 2 Scorecard alerts (#123 + #124) that explicitly
defer to v0.10.7. Layered on top: the Phase 2 (2026-05-27 continuation)
synthesis identified a 3-layer pre-push gate as the right structural
defense against the "ship-a-new-workflow-that-itself-trips-Scorecard"
pattern that produced #124.

**Strategic constraint** — Q3 2026 regulatory window:

| Event | Date | Days from 2026-05-27 |
|---|---|---|
| EU CRA reporting obligations live | 2026-09-11 | 107 |
| FedRAMP OSCAL mandate live | 2026-09-30 | 126 |
| Maryland MODPA (strictest US privacy law) | 2026-10-01 | 127 |
| CMMC Phase 2 Level 2 C3PAO mandatory | 2026-11-10 | 167 |

v0.11 federal-compliance theme (~6-8 weeks substantive) needs to land
**before** the CMMC Phase 2 trigger ideally — that's the
highest-leverage regulatory event for Evidentia's federal positioning.
Working backward: v0.11 must open no later than ~2026-08-15 to give a
6-week build cycle + 2-week buffer before 2026-11-10. v0.10.7 has a
2.5-month window before that backstop; the chosen 2-3-week scope
preserves ~2 months of v0.11 budget.

**Scope choice locked by Allen** (2026-05-27):

- Aggressive scope: Group A (code-quality polish) + Group D (Scorecard
  delta) + Pre-push Gate L2 (blocking layer only).
- Crosswalk hand-verification: **deferred** to v0.10.8 or v0.11 (the
  v0.10.6 `"self-attested-via-upstream"` disclaimer convention is honest).
- Pre-push Gate L1 (local Scorecard sweep) + L3 (warning-only): **deferred**.
  Rationale: L1 duplicates the scheduled CI Scorecard workflow with no
  new signal; L3's actionlint + online-pinact catches syntax errors
  that the GitHub Actions UI already surfaces. Marginal value of L1+L3
  doesn't justify ~1-2 extra weeks of v0.11 burn.

---

## 2. Pre-cycle state verification (2026-05-27 ~19:15 UTC)

Verified at session-resume after the v0.10.6 ship + post-ship cleanup:

| Check | Expected | Actual | Match |
|---|---|---|---|
| Branch | `main` | `main` | ✅ |
| Working tree | clean | clean | ✅ |
| Unpushed commits | 0 | 0 (post-`c5b8b75` push) | ✅ |
| Last shipped tag | `v0.10.6` @ `254277f` | `v0.10.6` @ `254277f` | ✅ |
| Test count | 3536 pass / 14 skip | 3536 pass / 14 skip | ✅ |
| mypy strict | 0 issues / 279 files | 0 issues / 279 files | ✅ |
| ruff | clean | clean | ✅ |
| Open code-scanning alerts | 2 (#123, #124) | 2 (#123, #124) | ✅ |
| Workflow audit FAIL count | 4 (sync-wiki.yml added since v0.10.6) | 4 | ✅ |
| Doc-health audit | 0 FAIL / 74 WARN | 0 FAIL / 74 WARN | ✅ |

The 4 audit FAILs are all legitimate write scopes that need `JUSTIFIED`
annotations in Phase D4. The 2 open Scorecard alerts close in Phase D1.

---

## 3. Backlog inventory

### Group A — v0.10.6 code-quality reviewer items (9 items)

| Item | Source | Priority | Phase |
|---|---|---|---|
| A1 — OSPS crosswalk regeneration script (`gen_osps_crosswalks.py`) | C5 Important #1 | HIGH | D2 |
| A2 — Hardcoded SHA reduction in crosswalk JSONs (~15 sites) | C5 Important #2 | HIGH | D2 |
| A3 — GitHub OSPS collector DRY pass (`_unknown_finding()` factory; ~280 LOC) | C6 Important #1 | HIGH | D3 |
| A4 — `_file_present_at_any` asymmetric error handling | C6 Important #2 | MEDIUM | D3 |
| A5 — `audit_workflow_permissions.py` advisory → blocking promotion | C7 bonus | HIGH | D4 |
| A6 — `yaml.safe_load` None-handling in audit_workflow_permissions.py | C7 Minor #1 | LOW | D1 |
| A7 — `translate_url()` unit-test extraction from `verify-osps-conformance.yml` | TODO marker in workflow | MEDIUM | D2 |
| A8 — `capability-matrix.md` stale absolute-path | C8 Minor #2 | LOW | D6 |
| A9 — README v0.10.4 entry style polish | Low-priority cleanup | LOW | D6 |

### Group B — OSPS Baseline conformance gaps — **DEFERRED**

| Item | Decision | Target |
|---|---|---|
| B1 — OSPS-LE-01.01 DCO sign-off (GOVERNANCE.md + second contributor) | Defer | v0.10.8 (needs external contributor) |
| B2 — OSPS-VM-05.03 `osv-scanner` CI gate (`verify-osv-scan.yml`) | Defer | v0.10.8 (small enough; could pull forward) |
| B3 — 5 OSPS crosswalk hand-verification (`"self-attested-via-upstream"` → `"hand-checked"`) | Defer | v0.11+ (SME-grade work) |

### Group C — Phase 2 deferred work

| Item | Phase | Decision |
|---|---|---|
| C1 — Pre-push gate L2 (blocking) | D5 | **LAND in v0.10.7** |
| C2 — Pre-push gate L1 (local Scorecard sweep) | — | Defer (low marginal value) |
| C3 — Pre-push gate L3 (warning) | — | Defer (low marginal value) |
| C4 — Wiki content fill (~15 stub pages) | D6 (optional) | Land 2-4 pages this cycle; drip thereafter |
| C5 — 15 research prompts fire | — | Selective only (per-decision trigger) |

### Group D — v0.10.6 Step 7 surfaced Scorecard delta (2 items)

| Item | Source | Phase |
|---|---|---|
| D1 — Close Scorecard alert #123 (pip install hash-pinning on `verify-osps-conformance.yml:67`) | Step 7.D | D1 |
| D2 — Close Scorecard alert #124 (`sync-wiki.yml` top-level `contents: write` → scope to job) | Step 7.D | D1 |

### Group E — v0.11 federal theme items — **NOT IN THIS CYCLE**

Documented in ROADMAP §v0.11 (KSI emission, DORA Art. 17 emit, AI
catalogs, EU AI Act↔ISO 42001 crosswalk, arXiv preprint, VEX publish,
VSA emit, DORA-metrics extractor). v0.10.7 stays disciplined to
hygiene + automation-debt + pre-push L2.

---

## 4. Execution sequence

### Phase D1 — `ci(hardening): close v0.10.6 Step 7.D Scorecard delta + audit-minor fixes`

**Type:** workflow patches + small Python guard. **Days 1.**

**Scope:**

- **D1.1** — Pin `pip install pyyaml==6.0.3` in `verify-osps-conformance.yml:66`
  to use `--hash=sha256:<official>` form. Source the hash from PyPI's
  pyyaml 6.0.3 distribution page or via `pip download pyyaml==6.0.3
  --no-deps --no-binary=:all:` locally + `sha256sum`. Closes
  Scorecard alert #123 (`PinnedDependenciesID`).
- **D1.2** — Move `contents: write` in `sync-wiki.yml` from top-level
  to the job (and ideally to a per-step `permissions:` override on the
  push step only). Top-level becomes `permissions: read-all`. Closes
  Scorecard alert #124 (`TokenPermissionsID`).
- **D1.3** — Add `yaml.safe_load` None-handling guard in
  `scripts/audit_workflow_permissions.py:25` (currently `data.get(...)`
  on a None result raises `AttributeError`). Closes A6.

**Per-commit validation gate:**

- `uv run pytest -q --tb=no`: expect 3536 pass / 14 skip (unchanged).
- `uv run mypy --strict packages/`: expect 0 issues / 279 files.
- `uv run ruff check packages/ tests/ scripts/`: expect clean.
- `uv run python scripts/audit_workflow_permissions.py`: expect 8 OK
  / 4 FAIL (unchanged baseline — these are addressed in D4).
- `uv run python scripts/check_docs_health.py --strict`: expect 0 FAIL.

**Commit:** `ci(hardening): close v0.10.6 Scorecard delta + workflow-audit yaml-None guard`

**Verification:**

```bash
# After push: wait ~3 min for Scorecard CI to re-run, then:
gh api repos/Polycentric-Labs/evidentia/code-scanning/alerts --jq \
  'map(select(.state=="open")) | length'
# Expected: 0 (down from 2).
```

---

### Phase D2 — `feat(catalogs): OSPS crosswalk regen script + SHA-constant extraction`

**Type:** new script + JSON updates + workflow helper extraction. **Days 2-3.**

**Scope:**

- **D2.1** — Create `scripts/catalogs/gen_osps_crosswalks.py`. Read the
  pinned upstream OSPS Baseline `guidelines[]` YAMLs (commit
  `ac6bbec8aecf51dce41f62712745f7949ab6bdeb` per
  `OSPS-CONFORMANCE.md`); regenerate the 5 OSPS crosswalk JSONs
  deterministically. Include the upstream commit SHA in a single
  module-level constant. Verify the regenerated JSONs round-trip
  byte-for-byte against the current shipped 5 (the regen is a
  validation gate, not a change). Closes A1.
- **D2.2** — Refactor the 5 OSPS crosswalk JSONs to source the upstream
  SHA from a single constant module rather than ~15 inline
  duplications. Land the constant as
  `packages/evidentia-core/src/evidentia_core/catalogs/data/mappings/_osps_upstream.py`
  (single source of truth: `OSPS_BASELINE_COMMIT_SHA = "ac6bbec8..."`).
  Update the regen script to read from there. Closes A2.
- **D2.3** — Extract `translate_url()` from the embedded Python in
  `verify-osps-conformance.yml:160-298` into a new module
  `scripts/verify_osps_conformance.py`. Move the workflow's embedded
  Python to call the module via `python -m scripts.verify_osps_conformance`.
  Add `tests/unit/test_verify_osps_conformance.py` covering the 6
  current URL shapes the workflow translates (issues, contents,
  releases, security advisories, code search, repo metadata). Closes A7.

**Per-commit validation gate:** (same as D1) plus:

- New unit tests: `uv run pytest tests/unit/test_verify_osps_conformance.py -v`.
- Verify the regenerated JSONs match the shipped JSONs:
  `uv run python scripts/catalogs/gen_osps_crosswalks.py --check`
  (exit 0 if match; non-zero on drift).

**Commits (3 separate for atomicity):**

1. `feat(scripts): gen_osps_crosswalks.py deterministic regenerator`
2. `refactor(catalogs): hoist OSPS upstream SHA to single-source constant`
3. `refactor(workflows): extract translate_url() to scripts/verify_osps_conformance.py + tests`

---

### Phase D3 — `refactor(collectors): GitHub OSPS DRY pass + error-handling fix`

**Type:** collector refactor + small behavioral fix. **Day 4.**

**Scope:**

- **D3.1** — Add `_unknown_finding()` factory function to
  `packages/evidentia-collectors/src/evidentia_collectors/github/osps.py`.
  Dedupe the ~280 LOC of `UNKNOWN`-branch boilerplate across the 12 of
  16 `populate_osps_*` helpers that share the pattern. Verify all 65
  existing tests still pass (the helpers' public behavior is
  unchanged; only internal structure is deduped). Closes A3.
- **D3.2** — Fix `_file_present_at_any` asymmetric error handling: 5xx
  errors currently silently FAIL the conformance check. Change to
  surface UNKNOWN on all-probes-failed (more honest signal: we don't
  know whether the file is present, not that it's definitively
  absent). Add 2-3 tests covering the 500/502/504 cases. Closes A4.

**Per-commit validation gate:** (same as D1) plus:

- All 65 GitHub OSPS collector tests still pass.
- New 5xx-error tests cover the asymmetric branch.

**Commit:** `refactor(collectors): DRY github/osps.py UNKNOWN branches + asymmetric-error fix`

---

### Phase D4 — `ci(hardening): audit_workflow_permissions.py blocking + JUSTIFIED annotation parser`

**Type:** Python tooling + workflow annotations + CI integration. **Days 5-6.**

**Scope:**

- **D4.1** — Extend `scripts/audit_workflow_permissions.py` with:
  - `--strict` flag that exits 2 on any FAIL (default behavior stays
    advisory exit 0 for backward compat).
  - `JUSTIFIED` annotation parser: any FAIL workflow with a
    `# JUSTIFIED: <reason>` comment at the top of the `permissions:`
    block downgrades to OK (with the reason logged).
  - JSON output mode (`--json`) for per-run audit-trail integration.
- **D4.2** — Add `# JUSTIFIED:` comments to the 4 current-baseline FAIL
  workflows:
  - `.github/workflows/action-smoke-test.yml` —
    `pull-requests: write` → `# JUSTIFIED: composite-action smoke test posts PR comments with the smoke results`
  - `.github/workflows/catalog-refresh.yml` —
    `issues: write` → `# JUSTIFIED: bot opens an issue when catalog drift is detected upstream`
  - `.github/workflows/evidentia.yml` —
    `pull-requests: write` → `# JUSTIFIED: composite-action e2e smoke posts PR comment with result summary`
  - `.github/workflows/sync-wiki.yml` —
    `contents: write` → `# JUSTIFIED: wiki sync needs to push to the .wiki.git remote` (only valid AFTER D1.2 scopes the write to job-level)
- **D4.3** — Add a `verify-workflow-perms.yml` GitHub Actions workflow
  that runs `uv run python scripts/audit_workflow_permissions.py
  --strict` on every PR + push to main. Promotes the check from
  advisory-locally to blocking-on-CI. SHA-pin the workflow actions
  per OSPS-VM-04 + pre-push gate L2 (Phase D5) coverage.
- **D4.4** — Update `docs/release-checklist.md` Step 5.D (pre-tag) to
  add `audit_workflow_permissions.py --strict` to the pre-tag gate
  list (operator-side defense-in-depth).

**Per-commit validation gate:** (same as D1) plus:

- `uv run python scripts/audit_workflow_permissions.py --strict`:
  expect exit 0 (all 4 FAIL workflows now JUSTIFIED).
- `uv run pytest tests/unit/test_audit_workflow_permissions.py -v`:
  new tests for `--strict`, `JUSTIFIED` parser, `--json` output.

**Commits (2 separate):**

1. `feat(scripts): audit_workflow_permissions.py --strict + JUSTIFIED parser + --json`
2. `ci(hardening): verify-workflow-perms.yml + 4 JUSTIFIED annotations`

Closes A5.

---

### Phase D5 — `feat(hooks): pre-push gate L2 (blocking layer)`

**Type:** new tooling — pre-commit framework integration + 6 pre-push
checks + bypass logging + new doc. **Days 7-12.**

**Scope** — per the Phase 2 spec, L2-only:

- **D5.1** — `.pre-commit-config.yaml` at repo root. Pre-commit
  framework v3.5.0+ with `stages: [pre-push]` for the L2 hooks.
- **D5.2** — `scripts/pre_push/check_action_pins.sh` — wraps
  `pinact run -check -no-api` (offline SHA-pin syntax validation; Go
  binary; latest v4.0.0). Pre-flight: `pinact` available in PATH or
  installable via `go install github.com/suzuki-shunsuke/pinact/cmd/pinact@latest`.
- **D5.3** — `scripts/pre_push/check_secrets.sh` — grep staged + tracked
  files for `.env*`, `*.pem`, `*.key`, `*.pfx`, AWS access-key patterns,
  GH PAT patterns. Block if found. (Hard rule per `~/.claude/CLAUDE.md`
  secret-handling protocol.)
- **D5.4** — `scripts/pre_push/check_changelog_present.py` — if any
  `pyproject.toml` version field changed in the push range, verify
  `CHANGELOG.md` has a `## [<new-version>]` block. Reuses logic from
  `verify-changelog.yml` (Evidentia v0.10.4 P5 lesson).
- **D5.5** — Reuse the existing
  `scripts/check_docs_health.py --strict` (now config-driven; just
  invoke it from a pre-push hook entry).
- **D5.6** — Reuse `scripts/audit_workflow_permissions.py --strict`
  (built in Phase D4; just invoke from a pre-push hook entry).
- **D5.7** — `scripts/pre_push/check_uv_lock_pin_drift.py` — detect
  third-party pin drift in `uv.lock` (closes the v0.10.0 F-V100-M1
  pattern: a workspace package bump shouldn't move third-party pins).
  Inspect the diff for any non-workspace package whose version moved.
- **D5.8** — `docs/pre-push-gate.md` NEW — documents the 3-layer
  architecture (with L1 + L3 explicitly marked DEFERRED), per-hook
  rationale, bypass mechanism (`EVIDENTIA_ALLOW_PRE_PUSH_BYPASS=1`
  + rationale env var or interactive prompt), bypass log location
  (`.local/hooks/pre-push-bypass.log` JSONL — already gitignored
  via `.local/`).
- **D5.9** — Bypass logging: each bypass writes a JSONL row to
  `.local/hooks/pre-push-bypass.log` with `{timestamp, user, branch,
  reason, cmd, head_commit}`. Bypass requires either the env var
  `EVIDENTIA_PRE_PUSH_BYPASS_REASON="..."` set OR an interactive
  prompt on `/dev/tty` (the framework needs to detect terminal
  availability).

**Per-commit validation gate:** (same as D1) plus:

- Test each pre-push hook with a deliberate failure case:
  - Synthesize an unpinned `uses: actions/checkout@v6` in a temp
    workflow → expect `check_action_pins` blocks.
  - Synthesize a staged `.env.local` file → expect `check_secrets`
    blocks. **Critical:** verify the script never echoes the file's
    contents (per Allen's secret-handling protocol).
  - Bump `version` in `pyproject.toml` without adding a CHANGELOG
    block → expect `check_changelog_present` blocks.
  - Bump `evidentia-core` workspace version + accidentally bump
    `urllib3` from `>=2.0` to `>=3.0` in `uv.lock` → expect
    `check_uv_lock_pin_drift` blocks.
- Run the full L2 gate against the current HEAD: expect ALL pass
  (we'd otherwise have broken our own policy).
- Verify `EVIDENTIA_ALLOW_PRE_PUSH_BYPASS=1` + rationale env var
  writes a row to the JSONL log + allows the push.

**Commits (3 separate for atomicity):**

1. `feat(hooks): pre-push gate L2 framework + .pre-commit-config.yaml`
2. `feat(hooks): pre-push gate L2 individual checks (pinact + secrets + changelog + uv-lock-drift)`
3. `docs(pre-push-gate): pre-push-gate.md NEW + bypass-protocol`

Closes C1.

---

### Phase D6 — `docs(polish): capability-matrix + README v0.10.4 + FULL wiki content fill`

**Type:** docs-only — substantial content authoring with triple-
validation per claim. **Days 13-19 (expanded scope per Allen 2026-05-27).**

**Scope expansion rationale:** Allen's 2026-05-27 directive: "Do it
all, no skip. Verify everything three times to ensure that it's
accurate." This expands D6 from the original 4-page drip to the full
~40-page wiki tree fill, with triple-validation discipline per
`evidentia_standing_directive_research_rigor.md` applied to every
load-bearing claim (verified against initial source + independent
cross-source + actual codebase/repo state).

**Scope:**

- **D6.1** — Fix `docs/capability-matrix.md` stale absolute-path
  reference (pre-existing from v0.10.4 snapshot). Closes A8.
- **D6.2** — Polish README v0.10.4 entry style to match the v0.10.5 +
  v0.10.2 bold-key-feature pattern exactly (low-priority cleanup).
  Closes A9.
- **D6.3** — Author + verify all currently-stubbed wiki pages.
  Triple-validate each load-bearing claim. Categorize by content type:

  **Category 6.3.A — Auto-generated mirrors (8 pages; thin sync script).**
  These pages MUST stay in sync with their canonical root-doc source.
  Land a small generator script `scripts/wiki/sync_mirrors.py` that
  copies-with-header-injection from canonical to wiki path. Mirrors:
  - `docs/wiki/6-project/api-stability.md` ← `docs/api-stability.md`
  - `docs/wiki/6-project/roadmap.md` ← `docs/ROADMAP.md`
  - `docs/wiki/6-project/changelog.md` ← `CHANGELOG.md`
  - `docs/wiki/6-project/eol.md` ← `EOL.md`
  - `docs/wiki/6-project/verification.md` ← `docs/verification.md`
  - `docs/wiki/6-project/security.md` ← `SECURITY.md`
  - `docs/wiki/6-project/governance.md` ← `GOVERNANCE.md`
  - `docs/wiki/6-project/contributing.md` ← `CONTRIBUTING.md`
  - `docs/wiki/6-project/deprecation-policy.md` ← `docs/deprecation-calendar.md`
  Wire into `sync-wiki.yml` workflow so mirrors auto-refresh on each
  push to canonical.

  **Category 6.3.B — Already-canonical relocations (4 pages).**
  Move `git mv` existing canonical docs into the wiki tree per the
  Phase 2 spec; restate as mirrors via 6.3.A pattern:
  - `docs/wiki/5-compliance/ocsf-mapping.md` ← `docs/ocsf-mapping.md` (NORMATIVE)
  - `docs/wiki/5-compliance/gemara-mapping.md` ← `docs/gemara-mapping.md` (NORMATIVE)
  - `docs/wiki/5-compliance/financial-sector-overlay.md` ← `docs/financial-sector-overlay.md`
  - `docs/wiki/5-compliance/contributing-a-catalog.md` ← `docs/contributing-a-catalog.md`

  **Category 6.3.C — Auto-generated reference pages (5 pages).**
  These derive from code state and benefit from regeneration on each
  release. Land `scripts/wiki/sync_reference.py`:
  - `docs/wiki/4-reference/cli.md` — derived from Click command tree
    via `evidentia --help` recursive walk (or manual maintenance if
    no auto-tool available — verify which CLI framework first).
  - `docs/wiki/4-reference/mcp-tools.md` — derived from
    `packages/evidentia-mcp/src/evidentia_mcp/server.py` `@server.tool()`
    decorator parse (the 13 MCP tools).
  - `docs/wiki/4-reference/configuration.md` — derived from
    `evidentia config schema` if such verb exists, else from
    pydantic-settings introspection.
  - `docs/wiki/4-reference/catalogs.md` — derived from
    `packages/evidentia-core/src/evidentia_core/catalogs/data/manifest.yaml`
    (92 catalogs by region/standard/version/last-update).
  - `docs/wiki/4-reference/crosswalks.md` — derived from the 13
    mappings JSONs (source/target frameworks, verification posture,
    row count).

  **Category 6.3.D — Hand-authored concept pages (6 pages, triple-validated).**
  These need genuine prose authoring + triple-validation per claim:
  - `docs/wiki/3-concepts/data-model.md` — every Pydantic schema
    (SecurityFinding, ControlGap, CrosswalkDefinition, EventAction
    enum, OCSF mapping); frozen-surface contract.
  - `docs/wiki/3-concepts/catalog-engine.md` — `_load_catalog_data`
    ext-dispatch pattern; manifest regeneration; schema validation.
  - `docs/wiki/3-concepts/crosswalk-engine.md` — CrosswalkDefinition
    schema (incl. v0.10.6 additive fields); OSCAL back-matter mapping.
  - `docs/wiki/3-concepts/evidence-integrity.md` — CIMD envelope
    structure; signing keys; verification chain; WORM backend interface.
  - `docs/wiki/3-concepts/frozen-surfaces-and-stability.md` —
    Public-API contract; append-only MCP tool surface; semver policy.
  - `docs/wiki/3-concepts/rbac-and-multi-tenancy.md` — Multi-tenant
    primitives from v0.9.7 (data+decision layer); v0.11+ CLI/REST wiring.

  **Category 6.3.E — Hand-authored guide pages (10 pages, triple-validated).**
  Task-oriented how-tos:
  - `docs/wiki/2-guides/run-gap-analysis.md` — CLI walkthrough; catalog
    selection; evidence-dir conventions; output formats; faithfulness threshold.
  - `docs/wiki/2-guides/ingest-ocsf.md` — Prowler / AWS Security Hub
    ingestion; v0.10.2 `--block-private-ips` flag; v0.10.1 trust-unmapped.
  - `docs/wiki/2-guides/emit-sarif.md` — SARIF 2.1.0 for CI gates;
    GitHub Code Scanning ingestion; severity mapping.
  - `docs/wiki/2-guides/emit-ocsf-detection.md` — OCSF Detection
    Finding class_uid 2004 emit; SIEM ingestion; sample queries.
  - `docs/wiki/2-guides/emit-cyclonedx-vex.md` — CycloneDX 1.6 VEX
    statements; supply-chain composition.
  - `docs/wiki/2-guides/manage-poam.md` — POA&M 5-state lifecycle;
    CLI verbs; OSCAL POA&M emit; Jira/ServiceNow integration.
  - `docs/wiki/2-guides/conmon-deployment.md` — CONMON cadence library
    + CLI; 7 bundled federal cadences; daemon vs read-only.
  - `docs/wiki/2-guides/sign-and-verify-cimd.md` — CIMD signing of
    evidence; verification recipe; WORM backend.
  - `docs/wiki/2-guides/air-gapped-install.md` — Wheelhouse pattern;
    offline catalog updates; GPG-only fallback.
  - `docs/wiki/2-guides/ci-integration.md` — GitHub Actions sample;
    GitLab CI sample; Jenkins sample.
  - `docs/wiki/2-guides/osps-self-assessment.md` — Walk through
    `OSPS-CONFORMANCE.md` + the `verify-osps-conformance.yml` CI gate.

  **Category 6.3.F — Hand-authored compliance pages (3 pages,
  triple-validated).**
  - `docs/wiki/5-compliance/catalog-inventory.md` — 92 catalogs
    enumerated by family + version pin + last-update date.
  - `docs/wiki/5-compliance/framework-conformance.md` — Standards
    Evidentia itself conforms to (OSPS Baseline + NIST 800-53
    self-assessment + OpenSSF Best Practices Silver).
  - `docs/wiki/5-compliance/crosswalk-index.md` — Browse all 13
    crosswalks (filter by source/target/verification posture).

  **Category 6.3.G — Index page refresh + FAQ (4 pages).**
  - `docs/wiki/6-project/faq.md` — frequent operator questions.
  - `docs/wiki/index.md` — refresh now that ~40 stub pages have
    content; remove the "stub" markers.
  - `docs/wiki/2-guides/index.md` — refresh task-quick-reference table.
  - `docs/wiki/4-reference/index.md` — refresh reference card layout.

  **Category 6.3.H — Per-package API docs (7 pages).**
  Auto-generated via `mkdocstrings` (already in the docs dependency
  group from v0.10.6). Add `scripts/wiki/sync_api_docs.py` that runs
  `mkdocs build` headless + extracts the per-package API HTML →
  markdown via a small post-processor. Targets:
  - `docs/wiki/4-reference/api/evidentia-core.md`
  - `docs/wiki/4-reference/api/evidentia-ai.md`
  - `docs/wiki/4-reference/api/evidentia-mcp.md`
  - `docs/wiki/4-reference/api/evidentia-collectors.md`
  - `docs/wiki/4-reference/api/evidentia-api.md`
  - `docs/wiki/4-reference/api/evidentia-eval.md`
  - `docs/wiki/4-reference/api/evidentia-integrations.md`

- **D6.4** — Re-run `check_docs_health.py --strict`: expect the WARN
  count to drop from 74 → ~5 (all wiki broken-link WARNs resolved;
  a small handful of legitimate external WARNs may remain).

- **D6.5** — Add `sync-wiki.yml` to auto-trigger the 3 sync scripts
  (mirrors / reference / API) on each push to main that touches the
  source canonical files.

**Triple-validation discipline (every load-bearing claim per category 6.3.D/E/F):**

1. **Initial source** — read the actual codebase / NIST OSCAL spec /
   OCSF schema / etc. at HEAD.
2. **Independent cross-source** — cite at least 1 external doc
   (specification PDF, upstream README, peer-reviewed paper) that
   confirms the claim.
3. **Codebase verification** — run a grep / test invocation that
   demonstrates the claim is true at HEAD. E.g., "13 MCP tools" must
   be confirmed by `grep -rn "@server.tool" packages/evidentia-mcp/`.

Claims that fail any of the 3 must be either (a) removed, (b) softened
to "as of v0.10.X" with the verified count, or (c) annotated `[claim
not independently verified — see §12 corrections-log]`.

**Per-commit validation gate:** (same as D1) plus:

- Wiki sync workflow (`sync-wiki.yml`) dry-run after each new batch.
- `mkdocs build --strict` clean (no broken refs).
- `check_docs_health.py` cross-link WARN count trends DOWN.

**Commits (multiple, batched per category):**

1. `feat(scripts): wiki sync_mirrors.py + 9 mirror pages`
2. `docs(wiki): relocate 4 canonical compliance docs + mirror generator extension`
3. `feat(scripts): wiki sync_reference.py + 5 reference pages`
4. `docs(wiki): 6 concept pages (triple-validated)`
5. `docs(wiki): 11 guide pages (triple-validated)`
6. `docs(wiki): 3 compliance pages + FAQ + index refreshes`
7. `feat(scripts): wiki sync_api_docs.py + 7 API reference pages`
8. `ci(wiki): wire 3 sync scripts into sync-wiki.yml`
9. `docs(polish): capability-matrix path + README style polish`

---

### Phase D7 — `chore(release): v0.10.7`

**Type:** version bump + release-prep docs. **Day 14.**

**Scope:**

- **D7.1** — Stamp `CHANGELOG.md` `## [0.10.7] - 2026-06-10` (date
  TBD at tag time) block with grouped entries by phase.
- **D7.2** — `uv run python scripts/bump_version.py --to 0.10.7`.
  Verify all 8 workspace packages bump atomically; verify third-party
  pins untouched (the v0.10.0 F-V100-M1 close-out + v0.10.1 P5
  workspace-allowlist verification).
- **D7.3** — `uv sync --all-extras --all-packages --all-groups` to
  regenerate the lockfile.
- **D7.4** — Dockerfile pin update (`COPY` + version tag).
- **D7.5** — `docs/positioning-and-value.md` §16 v0.10.7 entry.
- **D7.6** — `docs/ROADMAP.md` v0.10.7 PLANNED → SHIPPED transition
  + v0.10.8 PLANNED placeholder (carry the deferred items per the
  carry-forward backlog below).
- **D7.7** — Final per-commit validation gate. Create annotated tag
  `v0.10.7` locally (NOT pushed).

**Per-commit validation gate:** (same as D1) plus:

- Verify all 8 workspace packages report `0.10.7` via `uv tree |
  grep evidentia`.
- Verify `verify-changelog.yml` would PASS on this commit (run the
  validation locally via the script).

**Commit:** `chore(release): v0.10.7`

---

### 🛑 TIER-4 GATE TAG — `git push origin main v0.10.7`

**Canonical task:** mirrors the v0.10.6 TAG gate (impl-plan §Phase TAG).

**Effect:** publishes all unpushed v0.10.7 commits to `origin/main`
AND fires `.github/workflows/release.yml` (builds + publishes 8 PyPI
wheels with PEP 740 attestations + signs container with cosign
keyless OIDC + attaches SBOM to GitHub Release + emits SLSA Provenance
v1 attestation).

**LL-V105-1 partial-publish risk check before surfacing:** v0.10.7
introduces NO new PyPI projects (8 packages already at 0.10.6).
Pending-publisher check is N/A this cycle; flag in the surface
message so Allen knows.

**Procedure:**

1. Stop. Do NOT run `git push`.
2. Surface the exact 2-command sequence:
   `git push origin main` then `git push origin v0.10.7`.
3. Explain effect + reversibility ceiling (PyPI publishes are
   irreversible after ~30 min).
4. AskUserQuestion for explicit approval.
5. Run only after explicit "yes."

---

### Phase Step 7 — Post-tag verification

**Canonical tasks:** mirror v0.10.6 Step 7 (Tasks A-G).

| Step | Check | Expected |
|---|---|---|
| 7.A | PEP 740 attestation sweep | `pypi-attestations verify pypi` PASS on all 8 wheels |
| 7.B | Container smoke | `docker run ghcr.io/polycentric-labs/evidentia:v0.10.7 --version` → "Evidentia v0.10.7" |
| 7.C | osv-scanner on published SBOM | clean (modulo known-acknowledged carries) |
| 7.D | Scorecard delta | re-checks #123 + #124 closed; verify no new alerts |
| 7.E | Fresh-venv install | `python -m venv /tmp/v107 && /tmp/v107/bin/pip install evidentia==0.10.7` green |
| 7.F | cosign verify keyless OIDC + SLSA Provenance v1 | PASS |
| 7.G | Memory ship-doc + lessons-learned + CHANGELOG cross-check | landing in internal/private memory |

**Failure protocol:** if A–F fail, STOP, surface to Allen, do NOT
proceed to G. The ship-memory entry only lands if A–F PASS.

---

## 5. Standing approval-gate inventory

| Gate | When | Requires Allen approval? | Type |
|---|---|---|---|
| Per-commit (D1–D7) | Each `git commit` on `main` | NO — pre-approved direct-push | Local |
| TAG (push main + v0.10.7) | After D7 commit | YES — surface command, await yes | Tier-4 |

No external-repo PRs planned for this cycle (no T2-equivalent).
H-phase parallel skill patch only fires if a relevant LL-V107-*
lesson surfaces during execution.

---

## 6. v0.10.8 / v0.11 carry-forward backlog

Items explicitly deferred from v0.10.7 + reserved scope blocks:

### v0.10.8 candidates (next patch — narrow, ~3-5 days)

- B1 — OSPS-LE-01.01 DCO sign-off (needs external contributor; trigger
  on Allen identifying the second contributor)
- B2 — OSPS-VM-05.03 `osv-scanner` CI gate (`verify-osv-scan.yml`) —
  small enough that v0.10.8 could land it standalone or fold into v0.11
- Pre-push gate L1 (local Scorecard sweep) — defer-or-skip; revisit
  if a new pattern justifies it
- Pre-push gate L3 (warning-only) — defer-or-skip; revisit if a new
  pattern justifies it
- Wiki content drip (continuing — 2-4 pages per cycle until 15-page
  tree complete)
- Stylistic-only items from any reviewer batches

### v0.11 reserved (federal-compliance theme — ~6-8 weeks)

Per ROADMAP §v0.11 (locked 2026-05-24 audit synthesis):

- KSI (Key Security Indicators) emission — third output mode on
  `evidentia conmon` alongside the 7 bundled cadences
- `evidentia incident emit --format dora-art-17` — first Apache-2.0
  OSS DORA Art. 17 reference emitter
- `nist-sp-800-218a-ai-coding` Tier-B bundled catalog — 11 controls
  on AI-assisted code production
- AI-governance crosswalk enrichment 4-phase (ISO 27001:2022
  Amendment 1:2024 Climate; NIST AI 600-1 GenAI Profile;
  EU AI Act ↔ ISO/IEC 42001 crosswalk; CETS 225 stub)
- `evidentia vex publish --rekor` — Sigstore Rekor attestation
- VSA (Verification Summary Attestation) emit per SLSA v1.2
- Auto-generate `docs/security-review-vX.Y.Z.md` from per-run JSON
- DORA-metrics extractor across 30+ Evidentia releases
- arXiv preprint authored (6-8pp)
- Refresh `docs/integration-survey.md` competitive section
  post-operator-deep-dive
- B3 — 5 OSPS crosswalk hand-verification (SME-grade work; could
  fit into v0.11 cycle or stand-alone v0.10.x patch)
- Multi-tenant RBAC full CLI/REST wire (primitives shipped v0.9.7)

---

## 7. Standing-directive reminders

(Cite these to any implementer / reviewer subagent that's dispatched.)

- **No co-authorship.** Per `~/.claude/CLAUDE.md` absolute rule. No
  `Co-Authored-By: Claude`, no 🤖 footer, no Claude-attribution in
  any commit body, ever.
- **Triple-validate.** Every data claim verified against (1) initial
  source, (2) independent cross-source, (3) code/file/repo-state.
  Per `evidentia_standing_directive_research_rigor.md`.
- **No tier-vocab in publicly-facing surfaces.** Per
  `evidentia_no_tier_vocab_in_publicly_facing_surfaces.md` — applies
  to commit messages, tag bodies, release bodies, PR descriptions,
  issue titles. Enforced via the gitignored config + hook + skill
  Step 5.D.3 (post-v0.10.7-refactor architecture).
- **Push back on assumptions.** If a brainstorm-locked decision in
  this plan doesn't survive upstream verification, surface in a §12
  corrections-log entry before writing code.
- **Internal-only memory.** Pasha + home/RA/EIN address situation
  NEVER published in repo docs.
- **Tier-4 surface-and-await-yes.** Never paraphrase the command.
  Never silently substitute equivalents. Wait for explicit yes.
- **Secret handling.** Per `~/.claude/CLAUDE.md` §"Secret handling —
  mandatory protocol". Especially relevant for D5.3
  (`check_secrets.sh`): never echo a secret file's contents, even
  in error/diagnostic output.

---

## 8. §12 corrections log (empty at cycle-open)

If brainstorm-locked decisions in §4 above don't survive upstream
verification during execution, append entries here before writing the
affected code.

### §12.1 — (empty)
### §12.2 — (empty)

---

## 9. Lessons-learned skeleton (filled at cycle-close)

Pattern carried forward from v0.10.6 LL-V106-1. New LL entries land
in internal/private memory + `.local/pre-release-review/lessons-
learned.yaml` (gitignored). Cycle-open seed:

- **LL-V107-? (TBD)** — _to be added during/after execution if a
  surprise surfaces._

---

## 10. Cycle-close memory + ship-doc tracker

At end-of-cycle (after Step 7 G):

- Internal memory ship-doc entry — `evidentia_v0_10_7_shipped.md`
  pattern matching `evidentia_v0_10_6_shipped.md`
- Update `MEMORY.md` pointer with single-line summary (mindful of
  the 24.4KB index budget; consider trimming older shipped-cycle
  entries if budget is tight)
- Update `docs/security-review-v0.10.7.md` per-run JSON capture (the
  /pre-release-review skill emits this auto-generated artifact under
  the v5.1 deliverables contract)
- `.local/pre-release-review/runs/2026-MM-DDTHH-MM-SSZ-v0.10.7-prototype.json`
  — final per-run JSON for the audit trail
