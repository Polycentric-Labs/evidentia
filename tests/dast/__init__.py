"""Evidentia DAST suite (v0.9.5 P1.2).

Opt-in dynamic-application-security tests. NOT collected by the
default ``pytest tests/`` run — invoke explicitly via
``pytest tests/dast/``. See ``tests/dast/README.md`` for the
pre-flight + per-suite invocation guidance.
"""
